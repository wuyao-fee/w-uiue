import WDescriptions from "./src/descriptions.vue";

/* istanbul ignore next */
WDescriptions.install = function (Vue) {
  Vue.component(WDescriptions.name, WDescriptions);
};

export default WDescriptions;
